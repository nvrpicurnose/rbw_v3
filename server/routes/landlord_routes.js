const mongoose = require('mongoose');
const Landlord = mongoose.model('Landlord');

// GET /get_landlord
exports.get_landlord = function(req, res, next){
	
}

// POST /edit_landlord
exports.edit_landlord = function(req, res, next){
	
}